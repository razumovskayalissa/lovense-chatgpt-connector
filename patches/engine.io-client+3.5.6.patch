diff --git a/node_modules/engine.io-client/lib/socket.js b/node_modules/engine.io-client/lib/socket.js
index f062249..c20232b 100644
--- a/node_modules/engine.io-client/lib/socket.js
+++ b/node_modules/engine.io-client/lib/socket.js
@@ -8,5 +8,13 @@ var Emitter = require('component-emitter');
 var debug = require('debug')('engine.io-client:socket');
 var index = require('indexof');
 var parser = require('engine.io-parser');
-var parseuri = require('parseuri');
+// parseuri 0.x is vulnerable to regex denial of service. Its maintained API is
+// ESM-shaped, so adapt the safe parser to the small legacy shape Engine.IO 3 uses.
+var parseUri = require('parseuri').parseUri;
+var parseuri = function (uri) {
+  var parsed = parseUri(uri);
+  parsed.host = parsed.hostname;
+  parsed.path = parsed.pathname;
+  return parsed;
+};
 var parseqs = require('parseqs');
